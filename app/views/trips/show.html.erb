<h1><%= @trip.name %></h1>

<p>Total distance: <%= @trip.total_distance %></p>
<p>Average hiking distance: <%= @trip.average_trail_length %></p>
<p>Longest hiking distance: <%= @trip.longest_trail.length %></p>
<p>Shortest hiking distance: <%= @trip.shortest_trail.length %></p>

<h3>Trails</h3>
<% @trip.trails.each do |trail| %>
  <h4><%= trail.name %></h4>
  <ul>
    <li><%= trail.length %></li>
    <li><%= trail.address %></li>
  </ul>
<% end %>

<h3>Trail Names:</h3>
  <ul class="trail-list">
    <% @trip.trails.each do |trail| %>
        <li>trail.name</li>
    <% end %>
  </ul>
    
